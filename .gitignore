# Legacy pre-rebuild draft — huge original images, superseded by index.html.
# Kept on disk locally as a backup of the original work, not needed in the repo.
omid kheirabadi — drift_files/

# Claude Code local session config, not project content.
.claude/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
